export enum SelectTimeType {
  START,
  END
}

/**
 * 交通工具
 */
export enum VehicleType {
  Custom = 'custom',
  Drive = 'drive',
  Walk = 'walk',
  Metro = 'metro',
  Bus = 'bus'
}

/**
 * 输出文本类型
 */
export const OutputTextType = {
  /**
   * 出发
   */
  StartOff: '出发',

  /**
   * 到家
   */
  GoBackHome: '到家'
}
